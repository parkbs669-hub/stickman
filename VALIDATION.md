# 검증 결과 — 2026-09-10

## 실제 영상

- 입력: 기존 작업 폴더의 `alcaraz_forehand_v12_input.mp4`, 360×640, 24fps, 288프레임.
- MediaPipe를 실제 실행해 288프레임 모두에서 자세 검출. 기존 v12.4 캐시는 재사용하지 않음.
- `examples/alcaraz_upgrade_preview_stickman.mp4`: 원본 비교 화면, 720p, SSAA 2, 잔상, 한국어 제목, 앙상블 임팩트 추정.
- 0.5배속 결과: **808×720, 576프레임, 영상 24.000초, 오디오 24.000초**. FFprobe로 검증.
- 동일 입력의 새 캐시로 2배속·240p 재렌더: **144프레임, 6.000초**, `cache_used: true` 확인.
- 별도 4초 구간 추출 후 0.5배속 합성: **192프레임, 영상·오디오 모두 8.000초** 확인.
- 비교 영상의 프레임을 열어 원본/스틱맨 배치 및 한글 제목 표시를 확인.
- 검은색 12프레임 영상: 자세 미검출 오류를 출력하고 종료 코드 1 반환. 최종 MP4를 만들지 않음.
- TrackNet: 기존 가중치를 읽어 6프레임을 실제 추론. 텐서 크기 오류 없이 완료했으나 공은 검출하지 못함. 이는 실행 검증이며 추적 정확도 검증은 아님.

## 자동화 테스트

총 **36개** 테스트:

- 런타임 12개: 잘못된 인자, 입력 전체 해시, 캐시 구조/설정 검증, 원자적 저장, 인코딩 실패, 무음 영상, 짧은 오디오 패딩, 배속/구간 오디오 길이와 음정 등. 실제 FFmpeg 테스트 6개 포함.
- 모션 11개: 신뢰도 보존, 좌우 교정, 미검출 처리, SSAA 선 두께, 양손 그립, 정지 자세, FPS 기반 임팩트 간격, 빈/짧은/무음 오디오 등.
- 처리 흐름 10개: 수동 임팩트 우선순위/범위, 허위 중앙 임팩트 제거, 제한된 누락 보간, 장면 전환, 캐시 신뢰도와 잘린 캐시, 앙상블 호출, 안전한 실행 인자 등.
- TrackNet 3개: 히트맵 밝기 오버플로, 정규화 히트맵, 모델 출력 텐서 크기.

프로그램 폴더에서 재현:

```powershell
python -m unittest discover -s tests -v
```

Python에 torch가 없으면 선택 기능인 TrackNet 텐서 테스트 1개를 건너뜁니다. tkinter GUI는 실제 위젯 생성·기본값과 실행 인자 조립을 확인했고, PowerShell 실행기 구문 검사도 통과했습니다. GUI의 모든 버튼을 수동으로 눌러 검증한 것은 아닙니다.

## 실행 환경

Windows, Python 3.13, MediaPipe 0.10.35, OpenCV 4.13.0.92, NumPy 2.4.6, Pillow 11.3.0. TrackNet 실행 확인에는 설치된 PyTorch 2.12.0+cpu를 사용했습니다.

자동 임팩트 및 라켓 자세는 추정치입니다. 이 검증은 출력 파일·프레임·오디오 길이와 오류 처리의 정확성을 확인하며, 테니스 동작의 생체역학적 정확도를 보장하지 않습니다.
